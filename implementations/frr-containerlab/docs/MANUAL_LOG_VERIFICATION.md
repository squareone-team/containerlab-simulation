# Manual Log Verification

The maintained runbook moved to [practical/security/management-access-and-logging.md](./practical/security/management-access-and-logging.md).

Use the new page when you want:

- `syslog-server` listener checks
- `logger` injection commands from reachable nodes
- `grep` and `tail -f` commands for Ring 6 verification
- related Ring 3, Ring 4, and Ring 5 checks in the same place

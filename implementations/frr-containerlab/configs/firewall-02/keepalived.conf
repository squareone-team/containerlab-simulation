! Keepalived configuration for firewall-02 (BACKUP)
! VIP: 192.168.1.254/24 on eth1 (Ring 1 - Firewall Interface)

global_defs {
    router_id firewall-02
    script_user root
    enable_script_security
}

vrrp_script check_health {
    script "/usr/local/bin/check_firewall_health.sh"
    interval 2
    weight -20
    fall 2
    rise 2
}

vrrp_instance Ring1_VIP {
    state BACKUP
    interface eth1
    virtual_router_id 51
    priority 100
    advert_int 1

    authentication {
        auth_type PASS
        auth_pass ESI_Ring1_FW
    }

    virtual_ipaddress {
        192.168.1.254/24 dev eth1
    }

    track_script {
        check_health
    }

    notify_master "/usr/local/bin/notify_master.sh"
    notify_backup "/usr/local/bin/notify_backup.sh"
    notify_fault "/usr/local/bin/notify_fault.sh"
}

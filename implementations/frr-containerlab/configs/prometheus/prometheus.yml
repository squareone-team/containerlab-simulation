global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: frr-exporter
    metrics_path: /metrics/metrics
    fallback_scrape_protocol: PrometheusText0.0.4
    static_configs:
      - targets: ["frr-exporter:9342"]

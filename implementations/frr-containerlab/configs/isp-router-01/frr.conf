frr version 9.1
frr defaults traditional
hostname isp-router-01
log syslog informational
!
interface lo
 ip address 100.64.0.1/32
!
interface eth1
 ip address 203.0.113.2/30
!
interface eth2
 ip address 198.18.13.2/30
!
router bgp 64500
 no bgp ebgp-requires-policy
 bgp router-id 100.64.0.1
 neighbor 203.0.113.1 remote-as 65001
 neighbor 203.0.113.1 password ESI-BGP-EXTERNAL
 neighbor 198.18.13.1 remote-as 64400
 neighbor 198.18.13.1 password ESI-BGP-SECRET
 address-family ipv4 unicast
  network 100.64.0.1/32
  network 203.0.113.0/30
  neighbor 203.0.113.1 activate
  neighbor 203.0.113.1 default-originate
  neighbor 203.0.113.1 route-map BORDER-DEFAULT-ONLY out
  neighbor 198.18.13.1 activate
 exit-address-family
!
ip prefix-list BORDER-OUT seq 5 permit 0.0.0.0/0
ip prefix-list BORDER-OUT seq 10 deny any
route-map BORDER-DEFAULT-ONLY permit 10
 match ip address prefix-list BORDER-OUT
!
line vty
